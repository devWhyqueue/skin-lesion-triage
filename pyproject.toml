[project]
name = "skin-lesion-triage"
version = "0.1.0"
description = "Analysis utilities for the HAM10000 Bayesian triage report."
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.0",
    "numpy>=2.2.0",
]

[tool.uv]
package = false
